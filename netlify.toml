[build]
  command = "pip3 install -r requirements.txt && python3 scripts/fetch_news.py && python3 scripts/check_duplicate_cover_images.py && hugo --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.160.0"
  PYTHON_VERSION = "3.11"
  MISE_HUGO_EXTENDED = "false"
